import express from "express";

const router = express.Router();

router.get("/", async (req, res) => {
  /**
   * #route   GET /payments/v1/
   * #desc    
   */


  // TODO: ADD Validation + controller
});

export { router };
